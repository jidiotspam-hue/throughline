{
  "name": "throughline-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A proxy that serves its own front door: UI, auth, and fetching in one Worker.",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
